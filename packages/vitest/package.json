{
  "name": "@templ-project/vitest",
  "version": "0.3.0",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist/",
    "package.json"
  ],
  "scripts": {
    "build": "npx tsc -p .",
    "prebuild": "rimraf ./dist",
    "pretest": "npm run build",
    "test": "cross-env NODE_ENV=test vitest run",
    "test:watch": "cross-env NODE_ENV=test vitest"
  },
  "author": "Dragos Cirjan <dragos.cirjan@gmail.com>",
  "license": "MIT",
  "description": "EsLint configuration for Templ Project",
  "dependencies": {
    "@templ-project/tsconfig": "^0.4.0",
    "vitest": "^3.2.4"
  },
  "devDependencies": {
    "@templ-project/eslint": "^1.2.0",
    "rimraf": "^6.0.1"
  }
}
